{
  "name": "subly",
  "version": "0.1.0",
  "private": true,
  "description": "Watch any video in Picture-in-Picture with the site's subtitles shown in the floating window.",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "icons": "node scripts/gen-icons.mjs",
    "fixtures": "node tests/fixtures/server.mjs",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "typescript": "^5.6.0",
    "wxt": "^0.20.0"
  }
}
